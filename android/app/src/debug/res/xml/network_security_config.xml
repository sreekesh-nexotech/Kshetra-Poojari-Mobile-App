<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG ONLY. Permits cleartext to the hosts listed below and nowhere else;
  every other host still requires TLS. This file lives under src/debug/, so it
  is never merged into a release APK, and `EnvLoader.validate()` refuses a
  non-https API_BASE_URL in a release build regardless.

  10.0.2.2 is the dev machine as seen from the Android emulator.
  62.171.151.149 is the shared dev API, which is served over plain http on
  :8030. Drop it from this list the moment that host gets a certificate.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">62.171.151.149</domain>
    </domain-config>
</network-security-config>
